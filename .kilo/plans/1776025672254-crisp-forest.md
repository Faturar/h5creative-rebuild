# Plan: Implement 3-Slide Sidebar Slider on Booking Page

## Overview
Replace the static left sidebar content in the booking page with a 3-slide auto-rotating slider showing:
1. Livestream Service + Countdown Timer
2. Statistics Counter (Berapa brand yang sudah live)
3. Testimonials (Pengalaman Live H5 Creative)

## Current State Analysis

### Existing Component
- **File**: `app/components/booking/SidebarSlider.tsx` exists with the complete implementation
- **Issue**: Missing icon imports (Clock, TrendingUp, Quote, Star)
- **Issue**: Not being used in the booking page

### Current Booking Page
- **File**: `app/app/booking/page.tsx`
- **Lines 303-314**: Static left sidebar content with CountdownTimer component
- Uses `CountdownTimer` from `@/app/components/booking/CountdownTimer`

## Implementation Steps

### Step 1: Fix Missing Imports in SidebarSlider.tsx
**File**: `app/app/components/booking/SidebarSlider.tsx`

Add missing icon imports from lucide-react:
```typescript
import { Clock, TrendingUp, Quote, Star } from "lucide-react"
```

### Step 2: Update Booking Page to Use SidebarSlider
**File**: `app/app/booking/page.tsx`

1. **Import the SidebarSlider component** (around line 26):
   ```typescript
   import SidebarSlider from "@/app/components/booking/SidebarSlider"
   ```

2. **Replace the static sidebar section** (lines 302-314):
   - Remove: Static `<div>` with "Livestream Service" heading and `CountdownTimer`
   - Replace with: `<SidebarSlider />` component

3. **Remove unused import** (line 25):
   - Remove: `import CountdownTimer from "@/app/components/booking/CountdownTimer"`
   - (CountdownTimer is now part of SidebarSlider component)

### Step 3: Verify Component Structure

The SidebarSlider component includes:
- **Slide 1**: "Livestream Service" title, description, and CountdownTimer
- **Slide 2**: "Statistik Brand Live" title, description, and StatisticsCounter (shows 487+ brands)
- **Slide 3**: "Pengalaman Live H5 Creative" title, description, and rotating testimonials

### Step 4: Animation & Behavior

The slider already includes:
- Auto-slide every 5 seconds
- Smooth Framer Motion transitions
- Dot indicators for manual navigation
- Clickable dots to jump to specific slides

### Step 5: Test Considerations

- Verify auto-rotation works smoothly
- Check manual navigation via dots
- Ensure all countdown and counter animations work
- Test responsive layout (currently hidden on mobile, visible on lg+)
- Verify all 3 slides display correctly with proper animations

## Files to Modify

1. **app/app/components/booking/SidebarSlider.tsx** - Add missing imports
2. **app/app/booking/page.tsx** - Replace static sidebar with SidebarSlider component

## Expected Outcome

- Left sidebar will rotate between 3 slides every 5 seconds
- Users can manually navigate using dot indicators
- Each slide has its own gradient-styled content box
- Animations are smooth and professional
- Countdown timer, brand counter, and testimonials all function correctly
